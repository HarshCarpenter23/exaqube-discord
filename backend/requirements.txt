# Web / API
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# Database (async, honestly: asyncpg behind SQLAlchemy async)
sqlalchemy[asyncio]>=2.0,<2.1
asyncpg>=0.29,<0.31
alembic>=1.13,<2.0

# Validation / config
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
jsonschema>=4.21,<5.0   # validates the LLM's tool arguments against each plugin's schema

# SQL safety gate (AST parsing, not string matching)
sqlglot>=25.0,<28.0

# Artifact generation
openpyxl>=3.1,<4.0
python-pptx>=1.0,<2.0

# LLM provider (default: Groq, OpenAI-compatible). The provider sits behind an
# interface (app/agent/providers), so swapping to another is a new file + env var.
groq>=0.11,<1.0

# Logging
structlog>=24.1,<26.0

# Tests
pytest>=8.2,<9.0
pytest-asyncio>=0.23,<1.0
