[pytest]
# asyncio_mode=auto: plain `async def test_*` run without per-test markers.
asyncio_mode = auto
# Container runs as non-root and /app isn't writable; keep the cache in /tmp.
cache_dir = /tmp/pytest_cache
testpaths = tests
